version: "3.8"
services:
  zlack:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mongodb
    networks:
      - zlack-network

  mongodb:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - zlackdb:/data/db
    networks:
      - zlack-network

volumes:
  zlackdb:

networks:
  zlack-network:
    driver: bridge
